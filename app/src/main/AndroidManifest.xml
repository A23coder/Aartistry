<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="Aadhya Artistry"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.HomeActivity"
        tools:targetApi="31">
        <activity
            android:name=".presentation.edit.EditPage"
            android:exported="false" />
        <activity
            android:name=".presentation.subcategory.SubCat"
            android:exported="false" />
        <activity
            android:name=".presentation.categories.Subcategory"
            android:exported="false" />
        <activity
            android:name=".presentation.auth.Authentication"
            android:exported="false" />
        <activity
            android:name=".presentation.home.MainActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.admin.Admin"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.splash.Splash"
            android:exported="false"
            android:label="Aadhya Mehandi"
            android:launchMode="singleTask" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>